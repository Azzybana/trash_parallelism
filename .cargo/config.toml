[alias]
    t = "nextest run --no-fail-fast --config-file .cargo/nextest.toml"
    c = "llvm-cov nextest --no-fail-fast --config-file .cargo/nextest.toml --html --output-dir coverage --no-cfg-coverage"
    l = "llvm-cov nextest --no-fail-fast --config-file .cargo/nextest.toml --lcov --output-path lcov.info --no-cfg-coverage"

[build]
    rustdocflags = ["--default-theme", "trash", "--theme", "doc/trash.css"]
