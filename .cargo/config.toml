[alias]
    t = "nextest run --no-fail-fast --config-file .cargo/nextest.toml"
    c = "llvm-cov nextest --no-fail-fast --config-file .cargo/nextest.toml --html --output-dir coverage"

[build]
    rustdocflags = ["--default-theme", "trash", "--theme", "doc/trash.css"]
